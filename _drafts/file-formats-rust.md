# Binary file formats in Rust

## `bytemuck` and the `Pod` trait

- Same as the `object` crate

## `nom` and custom parsers

- No derives?
- `endio` ?

## External grammar

- e.g. `pest`, `kaitai-rs` (`abbau`)